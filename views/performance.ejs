<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instructor Dashboard</title>
    <link rel="stylesheet" href="/styles/performance.css">
</head>
<body>
    
    <div class="banner-v">
          <img src="/images/nick-fury-banner.png" alt="Banner Image">
        <h1>"Empower the next generation of heroes—your knowledge and skills are the foundation of their strength. Every course you create builds a future where courage, resilience, and wisdom shape a safer world. Lead boldly, inspire deeply, and watch as your legacy shapes S.H.I.E.L.D.’s finest."</h1>
    </div>

    <div class="ibg-v">
        <div class="header-v">
            <h1>S.H.I.E.L.D. Instructor Performance Dashboard</h1>
            <p>Welcome to the S.H.I.E.L.D. Training Academy Dashboard. Track your performance and earnings here.</p>
        </div>

<div class="pcontainer-v">
         
         <div class="dashboard-v">
            
            <div class="revenue-card-v">
                <h2>Total Revenue</h2>
                  <p class="revenue-amount-v">$<%= totalRevenue %></p>
                <small>All-time earnings</small>
            </div>

            
            <div class="metrics-v">
                <div class="metric-card-v">
                    <h3>Total Enrollments</h3>
                      <p class="metric-value-v"><%= totalEnrollments %></p>
                </div>
                <div class="metric-card-v">
                    <h3>Average Rating</h3>
                    <p class="metric-value-v">  <% if (typeof averageRating === 'number') { %>
                                <%= averageRating.toFixed(1) %> ★
                            <% } else { %>
                                N/A
                            <% } %></p>
                </div>
                
                <div class="metric-card-v">
                    <h3>Active Courses</h3>
                    <p class="metric-value-v">%= activeCourses %></p>
                </div>
            </div>
        </div>
    </div>

    <div class="courses-v">
        <h2>Your courses</h2>
        <div id="course-list-v" class="course-list-v">  <% courseList.forEach(course => { %>
                    <div class="course-card-v">
                        <h3><%= course.title %></h3>
                        <div class="course-info-v">
                            <p><strong>Avg Rating:</strong> 
                                <% if (typeof course.rating === 'number') { %>
                                    <%= course.rating.toFixed(1) %> 
                                <% } else { %>
                                    N/A
                                <% } %>
                            </p>
                            <p><strong>Enrollments:</strong> <%= course.enrollments %></p>
                            <p><strong>Revenue:</strong> $<%= course.revenue %></p>
                        </div>
                    </div>
                <% }) %>
           
        </div>
    </div>
    




    </div>
    <%-  include("partials/footer-i.ejs") %>
</body>
</html>
